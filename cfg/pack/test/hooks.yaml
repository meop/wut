---
operation:
  add:
    manager:
      brew:
        zsh:
          hooks:
            - brew tap meop/tap
        names:
          - hooks
      pacman:
        names:
          - hooks
  remove:
    manager:
      brew:
        zsh:
          hooks:
            - brew untap meop/tap
