---
environment:
  - virt_lxc_dir_path=/srv/virt/lxc
lxc:
  autostart: true
  config:
    apparmor:
      profile: unconfined
    pty:
      max: 1024
    seccomp:
      profile: ''
    mount:
      auto: 'cgroup:mixed proc:mixed sys:mixed'
    tty:
      max: 4
